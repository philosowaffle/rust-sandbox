// main method is reserved an always the entrypoint of the application
// Rust programs must be compiled before they can be run
// > rustc main.rs
// > .\main.exe
fn main() {

    // ! after the function name means this is a macro, not a function
    println!("Hello, world!");
}